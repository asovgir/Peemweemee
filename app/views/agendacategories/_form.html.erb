<%= form_with(model: agendacategory, local: true) do |form| %>
  <% if agendacategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(agendacategory.errors.count, "error") %> prohibited this agendacategory from being saved:</h2>

      <ul>
      <% agendacategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category, id: :agendacategory_category %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
