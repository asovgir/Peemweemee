<%= render 'shared/nav' %>
<p id="notice"><%= notice %></p>

<p>
	<td><%= image_tag @user.avatar unless @user.avatar.blank? %> </td>
</p>
<p>
  <strong>Username:</strong>
  <%= @user.username %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
